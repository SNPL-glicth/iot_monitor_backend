# Example env for iot_monitor_backend
DB_DIALECT=mssql
DB_HOST=localhost
DB_PORT=1434
DB_USER=sa
DB_PASSWORD=TU_PASSWORD
DB_NAME=iot_monitoring_system
PORT=3000

# CORS (separado por comas). Si no lo defines, se permiten los orígenes típicos de Vite en dev.
# Ej:
# CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
CORS_ORIGINS=
